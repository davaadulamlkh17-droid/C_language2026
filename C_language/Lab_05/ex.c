#include <stdio.h>
#define sz 101
void find(int a[], int n, int *addr, int *addr1){
    int i; 
    for ( i = 0; i < n; i++){
        if (a[i]%2==o){
            (*addr1)++;
        }else{
            (*addr1)++;
        }
    }
}
int main(){
    int n, i;
    int a[101];
    scanf("%d", &n);
    for ( i = 0; i < n; i++){
        scanf("%d", &a[i]);
    }
    int x_t=0;
    int x_i=0;
    find(i=0; i<n;i++);
    printf("%d %d\n", x_t,x_o);
    return 0;
}